// 1. Define the enum here
enum AnimationType { 
  fade, 
  slide, 
  scale, 
  rotate 
}

void main() {
  // 2. Now this will work and print "AnimationType.fade"
  // ignore: avoid_print
  print(AnimationType.fade);
}
